export declare class RegistrationServiceService {
    constructor();
    validateBankCode(bankCode: string): boolean;
    validateBranchCode(branchCode: string): boolean;
    validateAccountNo(accountNo: string): boolean;
}
